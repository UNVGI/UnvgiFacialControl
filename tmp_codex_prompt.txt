Execute only this single task (5.2 形式切替時の共通フィールド保持を実装する) according to the instructions in AGENTS.md (auto-loaded by Codex) and the spec documents in .kiro/specs/lipsync-animationclip-rework/ (requirements.md, design.md, tasks.md). Before starting, output the task name (5.2 形式切替時の共通フィールド保持を実装する). After completing the task, run UnityTestRunner to verify the result. If any file changes exist, run git add -A and then commit. The commit title must be the task name "5.2 形式切替時の共通フィールド保持を実装する" as-is. The commit body must contain a brief summary of what was done (files created/modified, key changes). Use a multi-line commit message with git commit -m "title" -m "body". Finally, output only OK or FAIL. tasks.txt is a user-managed file and must not be modified. After outputting OK or FAIL, complete the session without waiting for user input.
