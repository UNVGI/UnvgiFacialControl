# API リファレンス

FacialControl の公開 API を名前空間ごとに一覧します。

## 名前空間

- `Hidano.FacialControl.Domain` — ドメインロジック（Unity 非依存）
- `Hidano.FacialControl.Application` — ユースケース
- `Hidano.FacialControl.Adapters` — Unity 依存の実装（PlayableAPI / OSC / JSON / Input）
- `Hidano.FacialControl.Editor` — Editor 拡張
